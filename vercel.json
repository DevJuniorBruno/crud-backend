{
    "version": 2,
    "env": {
        "PRISMA_GENERATE_CMD": "prisma generate"
    },
    "builds": [
      {
        "src": "src/server.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "src/server.ts"
      }
    ]
  }
  